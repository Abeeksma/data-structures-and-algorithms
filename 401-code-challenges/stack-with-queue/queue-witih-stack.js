'use strict';

const stack = require('../stacks-and-queues/stack-and-queue');

class PsuedoQueue {
  constructor(){

  }
}

/* the plan is to have a stack1
if i want to enqueue something push it to stack1
if i want to dequeue something i pop then push each item to stack2
pop node, push node, repeat until stack 1 is empty
pop node off the stack2, 
then move everything back to stack1
pop push pop push ext ext 
*/

