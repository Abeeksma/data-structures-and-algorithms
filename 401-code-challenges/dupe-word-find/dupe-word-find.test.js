'use strict';

const reapeatedWord = require('./dupe-word-find');
const Hashtable = require ('../hashtable/hashtable');

describe('The reapeated Word function', () => {
  it('should find the first repeated word', () => {
    expect();
  });

});